apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc-local
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local
  resources:
    requests:
      storage: 1Gi

apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: test-pvc-replicated
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: replicated
  resources:
    requests:
      storage: 1Gi
